遗留问题

Git分布式，为啥还要搭建服务器？
Git服务器只是一个提供Git里面权限管理的内容，有助于更好的管理代码，从基础开发的角度看，确实不需要一个服务器之类的基础支持。

CPU占有率原理如何统计？

Linux中img格式文件是啥？
Img和windows中的ios文件大致相同，只是一个镜像，本质是许多文件的压缩版。

Maven仓库作用干嘛的

artifactory是干嘛的

KJrog是干嘛的

啥事安卓，原生安卓和其他厂商的安卓区别

Git如何进行分支管理？
Git本质是一个文件系统，用于更好的存放和管理文件，只是拉分支更方便而已，分支管理和使用的具体工具无关。

Linux父亲进程删除，子进程同样删除原因

Linux开机流程

OpenStack

Hypervisor

Docker 文件系统原理

Docker启动快速，得益于Go语言中的协程？

Andriod编译，为啥要翻墙

Socket通信

QNX,RTOS区别？

Java为啥要与其他c++之类的本地语言交互？不都是编译过后的二进制么？

都是下x86架构的cpu，linux编译的二进制能不能挪到windows下跑？如果有编好的二进制，不用操作系统能不能直接在cpu下执行?

进程间通信原理机制IPC

nginx/Apache作用，原理

SELinux是干嘛的

HTTP五种请求方法的区别与作用

netstat查询原理

Cgroup，NameSpace原理，线程调度原理

AUFS原理，docker文件系统原理

神经网络的第一个隐层神经元数量要大于输入层特征？
